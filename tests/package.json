{
  "name": "fse-ventilation-tool-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Known-answer regression tests for the FSE Ventilation Tool compute() engine.",
  "scripts": {
    "test": "node --test *.test.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
